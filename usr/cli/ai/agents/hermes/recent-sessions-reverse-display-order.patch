diff --git a/cli.py b/cli.py
index 186ab63..81d7fe5 100644
--- a/cli.py
+++ b/cli.py
@@ -6334,7 +6334,10 @@ class HermesCLI:
         print()
         print(f"  {'#':<3} {'Title':<32} {'Preview':<40} {'Last Active':<13} {'ID'}")
         print(f"  {'─' * 3} {'─' * 32} {'─' * 40} {'─' * 13} {'─' * 24}")
-        for idx, session in enumerate(sessions, start=1):
+        # Print oldest-to-newest so the most recent session stays closest to
+        # the prompt.  Keep the original numbers so /resume 1 still targets
+        # the most recent session, even though it is rendered at the bottom.
+        for idx, session in reversed(list(enumerate(sessions, start=1))):
             title = session.get("title") or "—"
             preview = (session.get("preview") or "")[:38]
             last_active = _relative_time(session.get("last_active"))
