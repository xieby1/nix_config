diff --git a/crates/goose/src/providers/formats/openai_responses.rs b/crates/goose/src/providers/formats/openai_responses.rs
index dc49ce1f1..53846f7a9 100644
--- a/crates/goose/src/providers/formats/openai_responses.rs
+++ b/crates/goose/src/providers/formats/openai_responses.rs
@@ -120,23 +120,27 @@ pub struct ResponseUsage {
 pub enum ResponsesStreamEvent {
     #[serde(rename = "response.created")]
     ResponseCreated {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         response: ResponseMetadata,
     },
     #[serde(rename = "response.in_progress")]
     ResponseInProgress {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         response: ResponseMetadata,
     },
     #[serde(rename = "response.output_item.added")]
     OutputItemAdded {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         output_index: i32,
         item: ResponseOutputItemInfo,
     },
     #[serde(rename = "response.content_part.added")]
     ContentPartAdded {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         item_id: String,
         output_index: i32,
         content_index: i32,
@@ -144,7 +148,8 @@ pub enum ResponsesStreamEvent {
     },
     #[serde(rename = "response.output_text.delta")]
     OutputTextDelta {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         item_id: String,
         output_index: i32,
         content_index: i32,
@@ -156,13 +161,15 @@ pub enum ResponsesStreamEvent {
     },
     #[serde(rename = "response.output_item.done")]
     OutputItemDone {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         output_index: i32,
         item: ResponseOutputItemInfo,
     },
     #[serde(rename = "response.content_part.done")]
     ContentPartDone {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         item_id: String,
         output_index: i32,
         content_index: i32,
@@ -170,7 +177,8 @@ pub enum ResponsesStreamEvent {
     },
     #[serde(rename = "response.output_text.done")]
     OutputTextDone {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         item_id: String,
         output_index: i32,
         content_index: i32,
@@ -180,14 +188,20 @@ pub enum ResponsesStreamEvent {
     },
     #[serde(rename = "response.completed")]
     ResponseCompleted {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         response: ResponseMetadata,
     },
     #[serde(rename = "response.failed")]
-    ResponseFailed { sequence_number: i32, error: Value },
+    ResponseFailed {
+        #[serde(default)]
+        sequence_number: Option<i32>,
+        error: Value,
+    },
     #[serde(rename = "response.function_call_arguments.delta")]
     FunctionCallArgumentsDelta {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         item_id: String,
         output_index: i32,
         delta: String,
@@ -196,14 +210,16 @@ pub enum ResponsesStreamEvent {
     },
     #[serde(rename = "response.function_call_arguments.done")]
     FunctionCallArgumentsDone {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         item_id: String,
         output_index: i32,
         arguments: String,
     },
     #[serde(rename = "response.refusal.delta")]
     RefusalDelta {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         item_id: String,
         output_index: i32,
         content_index: i32,
@@ -211,7 +227,8 @@ pub enum ResponsesStreamEvent {
     },
     #[serde(rename = "response.refusal.done")]
     RefusalDone {
-        sequence_number: i32,
+        #[serde(default)]
+        sequence_number: Option<i32>,
         item_id: String,
         output_index: i32,
         content_index: i32,
